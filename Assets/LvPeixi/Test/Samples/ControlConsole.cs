using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using LitJson;
using System.IO;

public class ControlConsole : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //var jsonFile = File.ReadAllText(Application.dataPath + "/GameConfig/InteractionConfig.json");
        //JsonData jsonData = JsonMapper.ToObject(jsonFile);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
